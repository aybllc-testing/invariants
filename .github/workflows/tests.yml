name: un-algebra-tests
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r requirements.txt
      - run: pytest -q tests
      - name: Compute repo tree hash
        run: python scripts/hash_tree.py --out reporting/REPO_TREE_SHA256.txt
      # - name: Generate SBOM (requires syft)
      #   uses: anchore/sbom-action/download-syft@v0.15.10
      # - run: syft packages dir:. -o spdx-json=reporting/SBOM.spdx.json
      # - name: Upload artifacts
      #   uses: actions/upload-artifact@v4
      #   with:
      #     name: reports
      #     path: |
      #       reporting/REPO_TREE_SHA256.txt
      #       reporting/SBOM.spdx.json
